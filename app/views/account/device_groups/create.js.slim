|
  $('table tbody').prepend('#{render('account/device_groups/element')}');
  $('#modalForm').modal('hide');
  $('##{@device_group.id}').addClass('bg-rgba-info').delay(1000).queue(function(){
    $(this).removeClass('bg-rgba-info').dequeue();
  });
